/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Rasm optimallashtirish uchun tashqi manbalar (kelajakda kerak bo'lsa)
  images: { remotePatterns: [] },
};

export default nextConfig;
